#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/C:/Users/bobo5/dipsa41cicd/node_modules/.store/@railway+cli@3.4.0/node_modules/@railway"
else
  export NODE_PATH="$NODE_PATH:$basedir/C:/Users/bobo5/dipsa41cicd/node_modules/.store/@railway+cli@3.4.0/node_modules/@railway"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../.store/@railway+cli@3.4.0/node_modules/@railway/cli/bin/railway.js" "$@"
else
  exec node  "$basedir/../.store/@railway+cli@3.4.0/node_modules/@railway/cli/bin/railway.js" "$@"
fi
